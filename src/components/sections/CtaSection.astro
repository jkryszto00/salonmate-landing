---
/**
 * CTA Section Component
 * Main call-to-action with waitlist form
 */
---

<section class="cta" id="waitlist">
  <div class="cta-content">
    <div class="cta-badge">Szukamy pierwszych 100 testerek</div>
    <h2 class="cta-title">Pomóż nam zbudować<br />coś dla Ciebie</h2>
    <p class="cta-desc">Dołącz do wczesnego dostępu. Dostaniesz darmowy test + Twój feedback ukształtuje produkt.</p>
    <form class="waitlist-form" id="waitlistForm">
      <input type="text" name="name" placeholder="Twoje imię" required minlength="3" maxlength="100" />
      <input type="email" name="email" placeholder="Twój adres email" required />
      <div class="cf-turnstile" data-sitekey="" data-theme="light" data-size="flexible"></div>
      <button type="submit">Dołączam do listy</button>
    </form>
    <div class="cta-trust">
      <span class="cta-trust-item">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
        </svg>
        Dane bezpieczne (RODO)
      </span>
      <span class="cta-trust-item">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
          <polyline points="22 4 12 14.01 9 11.01"></polyline>
        </svg>
        Zero spamu
      </span>
      <span class="cta-trust-item">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
          <polyline points="22 4 12 14.01 9 11.01"></polyline>
        </svg>
        Powiadomimy gdy wystartujemy
      </span>
    </div>
  </div>
</section>

<style>
  .cta {
    padding: 6rem 4rem;
    background: var(--color-cream-dark);
    text-align: center;
  }

  .cta-content {
    max-width: 600px;
    margin: 0 auto;
  }

  .cta-title {
    font-family: var(--font-serif);
    font-size: clamp(1.75rem, 4vw, 3rem);
    font-weight: 400;
    line-height: 1.15;
    margin-bottom: 1rem;
  }

  .cta-desc {
    color: var(--color-taupe);
    font-size: 1rem;
    margin-bottom: 2rem;
  }

  .waitlist-form {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    max-width: 380px;
    margin: 0 auto 1rem;
  }

  .waitlist-form input {
    width: 100%;
    padding: 1rem 1.25rem;
    border: 1px solid var(--color-soft-brown);
    background: white;
    font-family: inherit;
    font-size: 1rem;
    outline: none;
    transition: border-color 0.3s ease;
    text-align: center;
  }

  .waitlist-form input:focus {
    border-color: var(--color-warm-black);
  }

  .waitlist-form button {
    width: 100%;
    padding: 1.1rem 1.75rem;
    background: var(--color-warm-black);
    color: var(--color-cream);
    border: none;
    font-family: inherit;
    font-size: 1rem;
    font-weight: 500;
    cursor: pointer;
    transition: background 0.3s ease;
  }

  .waitlist-form button:hover {
    background: var(--color-accent);
  }

  .cta-badge {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(196, 113, 91, 0.15);
    color: #c4715b;
    font-size: 0.85rem;
    font-weight: 500;
    border-radius: 100px;
    margin-bottom: 1.5rem;
  }

  .cta-trust {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    flex-wrap: wrap;
    margin-top: 1rem;
  }

  .cta-trust-item {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-size: 0.8rem;
    color: var(--color-taupe);
  }

  .cta-trust-item svg {
    color: var(--color-success);
    flex-shrink: 0;
  }

  @media (max-width: 1024px) {
    .cta {
      padding-left: 2rem;
      padding-right: 2rem;
    }
  }

  @media (max-width: 768px) {
    .cta {
      padding: 4rem 1.5rem;
    }
  }

  @media (max-width: 380px) {
    .cta {
      padding-left: 1rem;
      padding-right: 1rem;
    }
  }
</style>
