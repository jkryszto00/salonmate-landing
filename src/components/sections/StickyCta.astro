---
/**
 * Sticky Mobile CTA Component
 * Appears on scroll for mobile devices
 */
---

<div class="sticky-cta" id="stickyCta">
  <a href="#waitlist">Zapisz się na listę oczekujących →</a>
</div>

<style>
  .sticky-cta {
    display: none;
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 1rem;
    background: white;
    box-shadow: 0 -4px 20px rgba(26, 24, 23, 0.1);
    z-index: var(--z-sticky, 99);
    transform: translateY(100%);
    transition: transform 0.3s ease;
  }

  .sticky-cta:global(.visible) {
    transform: translateY(0);
  }

  .sticky-cta a {
    display: block;
    width: 100%;
    padding: 1rem;
    background: var(--color-warm-black);
    color: var(--color-cream);
    text-align: center;
    text-decoration: none;
    font-weight: 600;
    font-size: 0.95rem;
    transition: background 0.3s ease;
  }

  .sticky-cta a:hover {
    background: var(--color-accent);
  }

  @media (max-width: 768px) {
    .sticky-cta {
      display: block;
    }
  }
</style>
