---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
}

const {
  title = 'SalonMate — AI Recepcjonistka dla Twojego Salonu',
  description = 'AI recepcjonistka, która odpowiada na wiadomości, umawia wizyty przez DM i zna Twój salon. Działa 24/7, nawet kiedy Ty odpoczywasz.',
  ogImage = 'https://salonmate.ai/og-image.jpg',
} = Astro.props;

const siteUrl = 'https://salonmate.ai';
---

<!doctype html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>

    <!-- Meta tags -->
    <meta name="description" content={description} />
    <meta name="keywords" content="AI recepcjonistka, salon beauty, automatyzacja, Instagram DM, Booksy, salon fryzjerski, salon kosmetyczny" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={siteUrl} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content="Odpowiada na DM-y 24/7, umawia wizyty, zna Twój cennik. Dołącz do waitlisty i przetestuj za darmo." />
    <meta property="og:image" content={ogImage} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>💬</text></svg>" />

    <!-- Preconnect for fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />

    <meta name="generator" content={Astro.generator} />

    <script is:inline type="text/partytown" src="https://www.googletagmanager.com/gtag/js?id=G-SSKLZEPN0K"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag("consent", "default", {
        ad_storage: "denied",
        ad_user_data: "denied",
        ad_personalization: "denied",
        analytics_storage: "denied",
      });

      gtag('config', 'G-SSKLZEPN0K');
    </script>
  </head>
  <body>
    <slot />
  </body>
</html>
